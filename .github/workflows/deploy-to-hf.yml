name: Deploy to HF Space

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Push to Hugging Face Space
        uses: huggingface/action-push-to-space@v1
        with:
          repo-id: abhi20033/houseprice-mlops
          repo-type: space
          token: ${{ secrets.HF_TOKEN }}
